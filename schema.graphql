enum Type {
  STAKE
  CLAIM
  UNSTAKE
}

type StakeLog @entity(immutable: true) {
  id: Bytes!
  amount: BigInt!
  amountEth: BigDecimal!
  address: Bytes!
  block: BigInt!
  tx: Bytes!
  date: BigInt!
  type: Type!
}
